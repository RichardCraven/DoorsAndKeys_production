const { InventoryManager } = require('./src/utils/inventory-manager.js');
// Wait, inventory-manager uses ES6 imports. I'll just write a script that runs via babel or similar, or I can just mock the logic.
